apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 8080  # в поде multitool слушает 8080
  type: ClusterIP
